[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "tidewindow"
version = "0.1.0"
description = "Marine-planning dashboard for Puget Sound kayaking and fly fishing windows."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "aiohttp",
    "fastapi",
    "python-dotenv",
    "textual",
    "uvicorn",
]

[project.scripts]
tidewindow = "main:main"

[tool.setuptools]
py-modules = [
    "main",
    "marine_config",
    "marine_cache",
    "marine_engine",
    "marine_regions",
    "marine_terminal",
    "noaa_client",
    "sun_times",
    "web_app",
]
